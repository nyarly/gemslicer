#!/usr/bin/env ruby

root      = File.expand_path('..', File.dirname(__FILE__))
config_ru = File.join(root, 'config_proxy.ru')
app_dir   = File.join(root, 'app')
lib_dir   = File.join(root, 'lib')

exec("rackup -I #{app_dir}:#{lib_dir} -r rubygems #{ARGV.join(' ')} #{config_ru}")
